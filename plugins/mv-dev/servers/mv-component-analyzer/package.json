{
  "name": "mv-component-analyzer",
  "version": "1.0.0",
  "description": "MCP server para analisis de componentes React/Next.js y cumplimiento del design system de MV",
  "main": "dist/index.js",
  "scripts": {
    "build": "esbuild src/index.ts --bundle --platform=node --target=node20 --outfile=dist/index.js --format=esm --banner:js=\"import { createRequire } from 'module'; const require = createRequire(import.meta.url);\"",
    "build:tsc": "tsc",
    "start": "node dist/index.js",
    "dev": "npx tsx src/index.ts"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.1",
    "zod": "^3.24.2"
  },
  "devDependencies": {
    "@types/node": "^22.13.1",
    "esbuild": "^0.24.2",
    "typescript": "^5.7.3"
  }
}
